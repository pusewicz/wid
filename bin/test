#!/usr/bin/env ruby -w
# frozen_string_literal: true

$LOAD_PATH.unshift File.expand_path('../lib', __dir__)

require 'wid'

code = '
  puts 1 + 3.14
'

Wid::Lexer.new(code)
tokens = []
while (tok = parser.next_token)
  tokens << tok
  puts tok.inspect
end

Wid::Parser.new(tokens)
